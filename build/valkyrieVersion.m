function v = valkyrieVersion()
% We are using semantic versioning: MAJOR.MINOR.PATCH
% Define the version components
    %major: a change that means it will work entirely different than
    %before, e.g. new input or output format
    %minor: a new feature added, backwards compatibility
    %patch: bug fixes only
    v = "1.0.0";   % OBS: edit here before new release!
end